---
id: cli-env-vars
title: Environment Variables
sidebar_position: 8
---

The CLI respects several environment variables.

## TRAINLOOP_DATA_FOLDER

Path to the data directory used by all commands.

**Default:** `./trainloop/data`

## TRAINLOOP_CONFIG_PATH

Path to the config file.

**Default:** auto‑discovered

## TRAINLOOP_LOG_LEVEL

Logging level (`DEBUG`, `INFO`, `WARNING`, `ERROR`).

**Default:** `INFO`

Set these variables before running commands or define them in your shell profile.
